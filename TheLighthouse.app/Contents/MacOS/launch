#!/bin/bash
# Find the app bundle location
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
APP_DIR="$(dirname "$(dirname "$SCRIPT_DIR")")"
HTML_FILE="$APP_DIR/lighthouse-launcher.html"

# Open the HTML file in the default browser
open "$HTML_FILE"
